agent:
  id: "yuriv"
  team: "team"
  runtime: aks
  interaction_model: "standalone"
  guardrail_profile: "strict"

identity:
  sp_client_id: "91dc80df-ac30-44ca-a2b0-b2189fbc05e0"
  keyvault_url: "https://kv-agenticdemo-poc.vault.azure.net/"

tools: []

evaluation:
  suite: default-strict
  ci_gate: true
  pass_threshold: 0.95
  datasets:
    - id: golden-dataset
      version: v1.0
      source: repo
      path: evals/datasets/golden-dataset.json

output_policy:
  violation_threshold: 5
  window_minutes: 1
  action: block
  notify_webhook: 

prompts:
  active_version: v1.0
  source: prompts/system.txt
