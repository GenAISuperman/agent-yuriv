apiVersion: v1
kind: Service
metadata:
  name: "{{AGENT_ID}}"
  namespace: "agents-{{TEAM_NAME}}"
spec:
  selector:
    app: "{{AGENT_ID}}"
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
