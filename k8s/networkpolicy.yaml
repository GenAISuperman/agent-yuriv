# Deny all ingress and egress by default
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: "deny-all-yuriv"
  namespace: "agents-team"
spec:
  podSelector:
    matchLabels:
      app: "yuriv"
  policyTypes:
    - Ingress
    - Egress
---
# Allow platform traffic only
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: "allow-platform-yuriv"
  namespace: "agents-team"
spec:
  podSelector:
    matchLabels:
      app: "yuriv"
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: apim
  egress:
    # HTTPS — APIM (tool calls + registry), Key Vault, ACR, Azure OpenAI
    - to: []
      ports:
        - protocol: TCP
          port: 443
    # DNS — required for name resolution
    - to: []
      ports:
        - protocol: UDP
          port: 53
